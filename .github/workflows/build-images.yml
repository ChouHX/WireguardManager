name: Build and Push Images

on:
  push:
    branches:
      - main
    paths:
      - 'internal/**'
      - 'main.go'
      - 'go.mod'
      - 'go.sum'
      - 'Dockerfile.backend'
      - 'frontend/**'
      - '.github/workflows/build-images.yml'
  workflow_dispatch:
    inputs:
      platforms:
        description: '目标平台（逗号分隔）'
        required: false
        default: 'linux/amd64'
      push_latest:
        description: '同时推送 latest 标签'
        type: boolean
        required: false
        default: true

env:
  REGISTRY: ghcr.io

jobs:
  build:
    name: build-${{ matrix.name }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: backend
            context: .
            dockerfile: ./Dockerfile.backend
          - name: frontend
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
          # 单容器镜像：Nginx + 后端 + 工具链打包在一起
          - name: allinone
            context: .
            dockerfile: ./Dockerfile.allinone

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # GHCR 要求镜像名全小写，而仓库名可能含大写字母
      - name: Normalize image name
        id: image
        run: echo "name=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ steps.image.outputs.name }}-${{ matrix.name }}
          tags: |
            type=ref,event=branch
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          # 手动触发时可指定多平台；push 触发时固定 amd64
          platforms: ${{ inputs.platforms || 'linux/amd64' }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # CI runner 在国外，前端构建走官方源更快
          build-args: |
            NPM_REGISTRY=https://registry.npmjs.org
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}
